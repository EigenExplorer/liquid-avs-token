"use strict";
/*
import { forgeCommand } from "../../../manager/src/utils/forge";
import { execAsync } from "../../../manager/src/utils";

export async function deployPriceUpdater() {
  const task = "PriceUpdater.s.sol:DeployPriceUpdater";
  const { stdout } = await execAsync(
    forgeCommand(task, process.env.ADMIN_PUBLIC_KEY!, "run()", "")
  );
  console.log("Price updater contracts deployed");
  console.log(stdout);
}
*/ 
